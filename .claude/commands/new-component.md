---
description: Create a new Astro component with boilerplate
---

Create a new Astro component named "$ARGUMENTS" in the `src/components/` directory.

The component should include:
- TypeScript interface for props
- Basic structure following the project's conventions
- Proper imports if needed

If no name is provided, ask the user for a component name.
