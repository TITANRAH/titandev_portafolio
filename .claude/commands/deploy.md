---
description: Deploy the project to Vercel
---

Deploy this project to Vercel by:

1. First, ensure all changes are committed to git
2. Run `git push` to push changes to the remote repository
3. If Vercel is connected to the repo, it will auto-deploy
4. Alternatively, use `vercel --prod` to deploy directly

Show the deployment status and provide the deployment URL when ready.
